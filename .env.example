# YouTube MCP Server Environment Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# GOOGLE OAUTH CONFIGURATION (REQUIRED)
# =============================================================================
# Get these from Google Cloud Console:
# 1. Go to https://console.cloud.google.com/
# 2. Create/select a project
# 3. Enable YouTube Data API v3
# 4. Create OAuth 2.0 credentials (Web Application)
# 5. Add redirect URI: http://localhost:8080/oauth/callback

GOOGLE_CLIENT_ID=your-google-oauth-client-id-here
GOOGLE_CLIENT_SECRET=your-google-oauth-client-secret-here
GOOGLE_REDIRECT_URI=http://localhost:8080/oauth/callback

# =============================================================================
# MCP SERVER CONFIGURATION
# =============================================================================
# Secret for JWT token signing (generate a random string)
# Example: openssl rand -base64 32
MCP_OAUTH_SECRET=your-random-secret-for-jwt-signing

# Public URL where your server is accessible
# For local development: http://localhost:8080
# For production: https://yourdomain.com
MCP_SERVER_URL=http://localhost:8080

# =============================================================================
# DATABASE CONFIGURATION (REQUIRED)
# =============================================================================
# PostgreSQL connection string
# Format: postgres://username:password@host:port/database?sslmode=disable
# 
# Local PostgreSQL example:
DATABASE_URL=postgres://mcp_user:your_password@localhost:5432/youtube_mcp_server?sslmode=disable

# =============================================================================
# SERVER CONFIGURATION (OPTIONAL)
# =============================================================================
# Port for the HTTP server
PORT=8080

# Environment (development, staging, production)
ENVIRONMENT=development
